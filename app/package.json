{
  "name": "SuperSprint",
  "version": "0.2.0",
  "description": "A web version of the old Atari game !",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:skip-check": "vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  },
  "dependencies": {
    "pixi.js": "^8.14.3"
  }
}
